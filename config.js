// Elvis Hot Seat – Global Highscore config (optional)
// Create a Supabase project, then paste values here and commit.
// If left empty, the app uses LOCAL highscores only.
window.EHS_CONFIG = {
  SUPABASE_URL: "",      // e.g. "https://xxxx.supabase.co"
  SUPABASE_ANON_KEY: ""  // anon public key
};
